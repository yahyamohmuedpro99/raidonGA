name: test first workflow

on:
  push: 
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: just print Hello world
        run: echo " Hello world"
      
    
